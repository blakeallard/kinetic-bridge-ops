# Codex Instructions

{{TASK_MARKER}}

Use this repo as the source of truth for `{{TASK_KEY}}`.

Before making changes, read:

1. `README.md`
2. `TASK.md`
3. `STATUS.md`
4. `AGENTS.md`

Rules:

- Prefer review, tests, validation, diffs, and small implementation tasks.
- Run relevant automated tests and record the exact verification performed.
- Update `STATUS.md` after meaningful work.
- Work from this repo path only.
- Do not modify Zoho runtime systems unless explicitly approved.
- Do not commit secrets, tokens, `.env` files, private customer data, or credential-bearing URLs.
- Do not commit or push without explicit approval. When approved, use a focused commit and create a branch and PR when possible.
